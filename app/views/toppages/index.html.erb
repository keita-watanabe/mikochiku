<% if logged_in? %>
  <p>ログイン完了ユーザー: <%= current_user.name %></p>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>ミコチクを始める</h1>
      <%= link_to '今すぐサインアップ', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>